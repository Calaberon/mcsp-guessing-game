var secretNumber = 8
const parseInt(guess) = prompt("Guess a number between 1 and 10") 
    if (guess === secretNumber) {
        alert("Correct!")
    } else if (guess < secretNumber) {
        alert("Higher")
    } else if (guess > secretNumber) {
        alert("Lower")
    }

